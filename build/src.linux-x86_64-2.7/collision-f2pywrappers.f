C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pywrapbetalevel (betalevelf2pywrap, lumimax, np
     &p1, npp10, npp2, npp20, pnum1, pnum2, betas, hglassold, circ, ex1,
     & ey1, ex2, ey2, clight, pi)
      external betalevel
      double precision lumimax
      integer npp1
      integer npp10
      integer npp2
      integer npp20
      double precision pnum1
      double precision pnum2
      double precision betas
      double precision hglassold
      double precision circ
      double precision ex1
      double precision ey1
      double precision ex2
      double precision ey2
      double precision clight
      double precision pi
      double precision betalevelf2pywrap, betalevel
      betalevelf2pywrap = betalevel(lumimax, npp1, npp10, npp2, np
     &p20, pnum1, pnum2, betas, hglassold, circ, ex1, ey1, ex2, ey2, cli
     &ght, pi)
      end


      subroutine f2pywrapio (iof2pywrap, iox)
      external io
      double precision iox
      double precision iof2pywrap, io
      iof2pywrap = io(iox)
      end

